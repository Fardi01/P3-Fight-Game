import Foundation


// La partie est terminer si tout les personnages d'une équipe sont mortes.
// Si c'est le cas, alors on affiche le joueur 

class GameOver {
    
    /// Si tous les points de vie des personnage de joueur 1 sont inferieur ou égal à 0
    /// Print(le joueur 2 a gagner)
    /// Print(le nombre de tour)
    /// Print(name, lives, gun) des deux joueurs.
    
    
    /// Si tous les points de vie des personnage de joueur 2 sont inferieur ou égal à 0
    /// Print(le joueur 1 a gagner)
    /// Print(le nombre de tour)
    /// Print(name, lives, gun) des deux joueurs.
}
