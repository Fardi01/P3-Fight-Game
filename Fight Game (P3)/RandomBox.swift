import Foundation


// Faire un coffret cadeau qui contient 1 armes (l'arme est different de l'arme du joueur)
// Le coffret peux apparaitre de manière aléatoire quand le jouer a lancé une première attaque.
// Si le coffret apparait devant le joueur, alors le joueur attaque avec l'arme du coffret

// MARK: - Random Box

class RandomBox: Weapon {
    
    /// Si le joueur 1 joue son tour: son arme d'attaque est remplacer aléatoirement par un nouveau (plus ou moins puissante)
    /// Print(Un nouveau coffre contient \(WeaponType) disponible)
}

